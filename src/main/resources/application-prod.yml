app:
  api:
    rate-limit:
      enabled: true
      window-seconds: 60
      max-requests: 120
  crawl:
    schedule-enabled: true
    internal-api:
      enabled: false

spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
